import React from 'react';
import styles from './spinner.module.css';

const Spinner = () => (
  <div className={`${styles.spinner} w-9 h-9 border-4 border-solid border-gray-800`} />
);

export default Spinner;
